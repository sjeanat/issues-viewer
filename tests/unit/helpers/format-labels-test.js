import { formatLabels } from 'issues-viewer/helpers/format-labels';
import { module, test } from 'qunit';

module('Unit | Helper | format labels');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatLabels([42]);
  assert.ok(result);
});
